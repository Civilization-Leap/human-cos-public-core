name: Public Core Release Bootstrap

on:
  push:
    branches:
      - release/public-core-s0-s1-v1

permissions:
  contents: write

jobs:
  registry-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Registered release workflow placeholder
        run: |
          echo "This safe placeholder exists on protected main only to register the release workflow before the audited release branch replaces it."
          echo "No Human-COS Runtime source or release artifact is transferred by this main-branch file."
