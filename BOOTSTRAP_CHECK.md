# Bootstrap required-check exercise

This temporary file exists only to trigger the Public Core bootstrap CI before Runtime source publication.

It contains no Human-COS Runtime source, protocol, schema, evidence, prompt, model output, or private engineering history.
